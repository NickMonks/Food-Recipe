export class Ingredient {

    // a shortcut can be simply give the access modifier in the constructor , so we dont need to define
    // the members/properties. 
    constructor (public name: string, public amount: string){
    }
}